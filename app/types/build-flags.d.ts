// Build-time constants injected by Vite `define` (see nuxt.config.ts).
// `__ANN_ENABLED__` mirrors hub.annotations.enabled and gates the tree-shaking
// of the manager-annotation layer in app/layouts/default.vue.
declare const __ANN_ENABLED__: boolean
